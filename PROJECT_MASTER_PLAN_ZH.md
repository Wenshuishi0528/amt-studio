# AMT Studio 总体实施计划

## 最终成品

成品是一个完全本地运行的 macOS 软件。用户拖入一首歌，软件可以输出主旋律和多条乐器 MIDI，并提供试听、低置信度提示、钢琴卷帘与人工修正。系统同时保留“演奏版”和“谱面版”，以免为了谱面整齐而丢失真实演奏信息。

## 技术主线

第一条路径直接对完整混音运行多乐器转录模型。第二条路径先分离人声、鼓、贝斯和其他，再使用专用模型。两条路径产生的结果统一成带来源和置信度的 JSONL 音符事件。融合系统比较它们的一致性、节拍关系、音域、旋律连续性和分轨质量，再输出最终轨道。

主旋律有独立流程。有主唱时优先使用人声分离加歌声转录，同时参考完整混音模型；无人声时保留多个主奏候选，并让用户指定乐器。这样全轨尚不完美时，最低功能仍然可以成立。

## 训练路线

先不从头训练大型模型。先完成强基线和人工参考集，找出可重复的错误。第一项自训练组件优先选择“候选音符重排序、纠错和置信度模型”，因为它可以利用多个现成模型的分歧和用户修改，数据需求低于重新学习全部声音特征。之后再根据证据微调歌声、贝斯、鼓或特定乐器模型。只有数据规模和基线结果充分支持时，才考虑更大的端到端训练。

## Mac 与 Hyak 分工

Mac 负责仓库、Codex、项目管理、试听、人工标注、界面和最终本地部署。Hyak 负责大模型、批量实验、参数比较和训练。Hyak 不成为最终软件的在线依赖。

## 防止项目失控的方法

Codex 每次只做 `tasks/` 中一个编号任务。根目录 `AGENTS.md` 会约束依赖隔离、隐私、评测和完成标准。所有未经验证的想法进入 `docs/BACKLOG.md`，不能顺手扩张当前任务。每个阶段由质量门槛控制，不能用“看起来能跑”代替盲测指标和人工修正时间。
