# Beat This worker

Beat/downbeat candidate used for score timing. Keep native beat/downbeat times before any tempo smoothing.

Command pattern:

```bash
beat_this path/to/audio.file -o path/to/output.beats
```

Do not quantize note events until raw note metrics have been computed.
